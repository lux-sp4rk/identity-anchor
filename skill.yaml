name: identity-anchorer
version: 1.1.0
description: "High-density identity compression using the Triple Anchor system (MBTI/Zodiac/Enneagram). Minimizes token bloat while maximizes persona stability."
author: "luxsp4rk"
category: "persona/utility"
hooks:
  - on_startup: "ingest IDENTITY.md"
  - on_drift: "refresh_anchors"
files:
  - IDENTITY.md
  - SOUL.md (optional)
keywords: [identity, compression, persona, anchor, token, optimization]
commands:
  configure: "python3 $SKILL_DIR/configure.py"
  anchor: "bash $SKILL_DIR/anchor.sh"